console.log(require('fs').readFileSync(require('path').join(__dirname, 'input.txt')).toString().split('\n').map(g => [g.split(':')[1].split('|')[0].trim().match(/[0-9]+/gi).map(Number), g.split(':')[1].split('|')[1].trim().match(/[0-9]+/gi).map(Number)].reduce((w, s) => s.reduce((acc, sn) => w.includes(sn) ? !acc ? 1 : acc * 2 : acc, 0))).reduce((acc, v) => acc + v));